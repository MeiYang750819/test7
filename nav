/* --------配色／字級設定區（可自行調整）-------- */
/* <======== 檔案用途：導覽列獨立視覺設定 ======== > */

/* ================================================================
【 🔘 NAV TABS CONFIG - 導覽列獨立視覺 】
描述：負責三個分頁切換按鈕的固定位置、層級與點擊狀態。
================================================================ */

.nav-tabs { 
  display: flex;        /* 啟用彈性佈局，讓三個按鈕平分寬度並排 */
  background: #1a1a1a;  /* 配合暗黑模式，背景改為極深灰 */
  position: sticky;     /* 鎖定在頁面上方不動 */
  top: calc(45px * 3);  /* 黏在三層狀態列下方 (45px*3=135px的位置) */
  z-index: 90;          /* 層級設定：確保低於上方狀態列(100)，但高於下方內容區 */
  box-shadow: 0 2px 5px rgba(0,0,0,0.5); /* 底部陰影加深以融入暗黑背景 */
  border-bottom: 1px solid rgba(255,255,255,0.05); /* 底部增加極淡分隔線 */
}

.tab-btn { 
  flex: 1;                  /* 三個按鈕各佔 33.3% 寬度 */
  text-align: center;       /* 文字水平居中 */
  text-decoration: none;    /* 去除超連結預設底線 */
  padding: 15px 0;          /* 按鈕上下內邊距，控制按鈕整體高度 */
  font-weight: bold;        /* 字體加粗 */
  color: #888888;           /* 未選中時的文字顏色改為淺灰 */
  font-size: 16px;          /* 字體大小設定 */
  border-bottom: 3px solid transparent; /* 預留 3px 的底線位置，防止切換時畫面跳動 */
  transition: all 0.2s ease; /* 切換分頁時的顏色平滑過渡動畫 */
}

/* 當前被選中 (Active) 的分頁標籤樣式 */
.tab-btn.active { 
  color: #FFFFFF; /* 選中時文字亮白 */
  border-bottom: 3px solid #FFFFFF; /* 選中時底部粗線亮白 */
}

/* 手機點擊效果優化 */
.tab-btn:active {
  background-color: rgba(255,255,255,0.05); /* 手指觸碰瞬間，給予淡淡的白色反饋背景 */
}
